/**
 * Created by mongolrgata
 */

define(['BinaryFile'], function (BinaryFile) {
    /**
     * @param {BinaryFile} binaryFile
     * @constructor
     */
    var PNA = function PNA(binaryFile) {

    };

    return PNA;
});
